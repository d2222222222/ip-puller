script must be run as sudo (sudo bash ippuller.sh)
dependency is tcpdump
----------------------------
ubuntu / debian
sudo apt-get install tcpdump

arch
sudo pacman -S tcpdump
-----------------------------
